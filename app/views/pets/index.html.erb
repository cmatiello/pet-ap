<div class="container-fluid">
  <div class="col-10">
      <div class="info">
        <h3>Esses são todos os pets encontrados</h3>
        <p>Se tiver qualquer informação sobre os donos desses animais entre em contato</p>
      </div>
      <div class="container pets-list">
        <% @pets.each do |pet| %>
          <div class="card">
            <% if pet.picture.attached? %>
              <%= image_tag url_for(pet.picture), alt: "pet", class: "card-img-top" %>
            <% else %>
              <%= image_tag "/images/pet.png", alt: "pet", class: "card-img-top" %>
            <% end %>
            <div class="card-body">
              <div>
                <div class="card-title">
                  Breed: <%= pet.breed %>
                </div>
                <div class="card-text">
                  Size: <%= pet.size %>
                </div>
              </div>
              <div class="btn-group btn-group-toggle">
                <%= link_to 'Show', pet_path(pet), class: "btn btn-secondary btn-sm" %>
                <%= link_to 'Edit', edit_pet_path(pet), class: "btn btn-secondary btn-sm" %>
                <%= link_to 'Destroy', pet_path(pet),
                        method: :delete,
                        data: { confirm: 'Are you sure?' }, class: "btn btn-secondary btn-sm" %>
              </div>
            </div>
          </div>
        <% end %>
      </div>
      <p>Have you found a lost pet? <%= link_to 'Post here', new_pet_path %> </p>

  </div>
</div>
